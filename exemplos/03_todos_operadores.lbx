int a;
int b;
int c;
boolean t;
boolean u;

a = 10;
b = 3;
c = (a + b) - (a * b) / (a % b);    // + - * / %
t = !(a == b) && (a != b);          // ! == !=
u = (a > b) && (a >= b) || (a < b) || (a <= b); // > >= < <= && ||

if (t || u) { write("ok\n"); } else { write("nope\n"); }

void main() { return; }
